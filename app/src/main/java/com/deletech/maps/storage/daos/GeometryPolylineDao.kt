package com.deletech.maps.storage.daos
import androidx.lifecycle.LiveData
import androidx.room.*
import com.deletech.maps.models.GeometryPolygon
interface GeometryPolylineDao {
    /*
    @Query("SELECT *FROM GeometryPolygon LIMIT 1")
    fun getGeometryLine(): LiveData<GeometryPolygon>
    @Query("SELECT * FROM GeometryPolygon LIMIT 1")
    fun fetchGeometryLine(): GeometryPolygon
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertGeometryLine(model: GeometryPolygon)
    @Update
    fun updateGeometryLine(model: GeometryPolygon)
    @Delete
    fun deleteGeometryLine(model: GeometryPolygon)
    @Query("DELETE FROM Geometry")
    fun delete()

     */
}