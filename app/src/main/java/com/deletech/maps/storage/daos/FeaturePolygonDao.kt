package com.deletech.maps.storage.daos

import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import com.deletech.maps.models.Feature
import com.deletech.maps.models.FeaturePolygon

interface  FeaturePolygonDao {
    /*
    @Query("SELECT * FROM FeaturePolygon")
    fun getAll():List<FeaturePolygon>
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertAll(model: List<FeaturePolygon>)
    @Query("DELETE FROM FeaturePolygon")
    fun delete()

     */
}