package com.deletech.maps.storage.daos
import androidx.lifecycle.LiveData
import androidx.room.*
import com.deletech.maps.models.Point
interface PointDao {
    /*
    @Query("SELECT *FROM Point LIMIT 1")
    fun getPoint(): LiveData<Point>
    @Query("SELECT * FROM Point LIMIT 1")
    fun fetchPoint(): Point
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertPoint(model: Point)
    @Update
    fun updatePPoint(model: Point)
    @Delete
    fun deletePoint(model: Point)
    @Query("DELETE FROM Point")
    fun delete()

     */
}