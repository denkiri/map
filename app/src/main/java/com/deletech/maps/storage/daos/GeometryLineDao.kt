package com.deletech.maps.storage.daos
import androidx.lifecycle.LiveData
import androidx.room.*
import com.deletech.maps.models.Geometry
interface GeometryLineDao {
    /*
    @Query("SELECT *FROM Geometry LIMIT 1")
    fun getGeometryLine(): LiveData<Geometry>
    @Query("SELECT * FROM Geometry LIMIT 1")
    fun fetchGeometryLine(): Geometry
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insertGeometryLine(model: Geometry)
    @Update
    fun updateGeometryLine(model: Geometry)
    @Delete
    fun deleteGeometryLine(model: Geometry)
    @Query("DELETE FROM Geometry")
    fun delete()

     */
}